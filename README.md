# local-firewall-manager
A Python-based dynamic firewall manager that controls outgoing network traffic using iptables. It resolves domain names, updates firewall rules based on trusted IPs/domains, and logs blocked traffic to a file and MySQL. It supports automatic updates to whitelist every 5 minutes for real-time security.
